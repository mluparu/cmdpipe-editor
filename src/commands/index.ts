export { CommandHandler } from './commandHandler';

// Re-export types for convenience
export type { TaskDefinition, TaskExecutionResult } from '../types/taskTypes';
export type { InsertionResult } from '../types/extensionTypes';